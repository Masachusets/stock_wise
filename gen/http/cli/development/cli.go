// Code generated by goa, DO NOT EDIT.
//
// development HTTP client CLI support package
//
// Command:
// $ goa gen github.com/Masachusets/stock_wise/design

package cli

import (
	"flag"
	"fmt"
	"net/http"
	"os"

	assignmentsc "github.com/Masachusets/stock_wise/gen/http/assignments/client"
	cardsc "github.com/Masachusets/stock_wise/gen/http/cards/client"
	departmentsc "github.com/Masachusets/stock_wise/gen/http/departments/client"
	equipmentsc "github.com/Masachusets/stock_wise/gen/http/equipments/client"
	nomenclaturesc "github.com/Masachusets/stock_wise/gen/http/nomenclatures/client"
	waybillsc "github.com/Masachusets/stock_wise/gen/http/waybills/client"
	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"nomenclatures (list|get)",
		"departments (list|get)",
		"cards (list|get|create|update|delete)",
		"equipments (list|get|create|update|delete)",
		"waybills (list|get|create|sign|archive|delete)",
		"assignments (list|get)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + " " + "nomenclatures list" + "\n" +
		os.Args[0] + " " + "departments list" + "\n" +
		os.Args[0] + " " + "cards list" + "\n" +
		os.Args[0] + " " + "equipments list --status \"broken\" --nomenclature-id 1958819844 --location \"Laboriosam nemo rerum et aspernatur quia.\" --search \"Voluptas quia minus soluta.\"" + "\n" +
		os.Args[0] + " " + "waybills list" + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		nomenclaturesFlags = flag.NewFlagSet("nomenclatures", flag.ContinueOnError)

		nomenclaturesListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		nomenclaturesGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		nomenclaturesGetIDFlag = nomenclaturesGetFlags.String("id", "REQUIRED", "ID номенклатуры")

		departmentsFlags = flag.NewFlagSet("departments", flag.ContinueOnError)

		departmentsListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		departmentsGetFlags    = flag.NewFlagSet("get", flag.ExitOnError)
		departmentsGetCodeFlag = departmentsGetFlags.String("code", "REQUIRED", "Код подразделения")

		cardsFlags = flag.NewFlagSet("cards", flag.ContinueOnError)

		cardsListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		cardsGetFlags      = flag.NewFlagSet("get", flag.ExitOnError)
		cardsGetNumberFlag = cardsGetFlags.String("number", "REQUIRED", "Номер карточки")

		cardsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		cardsCreateBodyFlag = cardsCreateFlags.String("body", "REQUIRED", "")

		cardsUpdateFlags      = flag.NewFlagSet("update", flag.ExitOnError)
		cardsUpdateBodyFlag   = cardsUpdateFlags.String("body", "REQUIRED", "")
		cardsUpdateNumberFlag = cardsUpdateFlags.String("number", "REQUIRED", "Номер карточки")

		cardsDeleteFlags      = flag.NewFlagSet("delete", flag.ExitOnError)
		cardsDeleteNumberFlag = cardsDeleteFlags.String("number", "REQUIRED", "Номер карточки")

		equipmentsFlags = flag.NewFlagSet("equipments", flag.ContinueOnError)

		equipmentsListFlags              = flag.NewFlagSet("list", flag.ExitOnError)
		equipmentsListStatusFlag         = equipmentsListFlags.String("status", "", "")
		equipmentsListNomenclatureIDFlag = equipmentsListFlags.String("nomenclature-id", "", "")
		equipmentsListLocationFlag       = equipmentsListFlags.String("location", "", "")
		equipmentsListSearchFlag         = equipmentsListFlags.String("search", "", "")

		equipmentsGetFlags               = flag.NewFlagSet("get", flag.ExitOnError)
		equipmentsGetInventoryNumberFlag = equipmentsGetFlags.String("inventory-number", "REQUIRED", "Инвентарный номер")

		equipmentsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		equipmentsCreateBodyFlag = equipmentsCreateFlags.String("body", "REQUIRED", "")

		equipmentsUpdateFlags               = flag.NewFlagSet("update", flag.ExitOnError)
		equipmentsUpdateBodyFlag            = equipmentsUpdateFlags.String("body", "REQUIRED", "")
		equipmentsUpdateInventoryNumberFlag = equipmentsUpdateFlags.String("inventory-number", "REQUIRED", "Инвентарный номер")

		equipmentsDeleteFlags               = flag.NewFlagSet("delete", flag.ExitOnError)
		equipmentsDeleteInventoryNumberFlag = equipmentsDeleteFlags.String("inventory-number", "REQUIRED", "Инвентарный номер")

		waybillsFlags = flag.NewFlagSet("waybills", flag.ContinueOnError)

		waybillsListFlags = flag.NewFlagSet("list", flag.ExitOnError)

		waybillsGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		waybillsGetIDFlag = waybillsGetFlags.String("id", "REQUIRED", "ID накладной")

		waybillsCreateFlags    = flag.NewFlagSet("create", flag.ExitOnError)
		waybillsCreateBodyFlag = waybillsCreateFlags.String("body", "REQUIRED", "")

		waybillsSignFlags  = flag.NewFlagSet("sign", flag.ExitOnError)
		waybillsSignIDFlag = waybillsSignFlags.String("id", "REQUIRED", "ID накладной")

		waybillsArchiveFlags  = flag.NewFlagSet("archive", flag.ExitOnError)
		waybillsArchiveIDFlag = waybillsArchiveFlags.String("id", "REQUIRED", "ID накладной")

		waybillsDeleteFlags  = flag.NewFlagSet("delete", flag.ExitOnError)
		waybillsDeleteIDFlag = waybillsDeleteFlags.String("id", "REQUIRED", "ID накладной")

		assignmentsFlags = flag.NewFlagSet("assignments", flag.ContinueOnError)

		assignmentsListFlags           = flag.NewFlagSet("list", flag.ExitOnError)
		assignmentsListEquipmentIDFlag = assignmentsListFlags.String("equipment-id", "", "")
		assignmentsListIsActiveFlag    = assignmentsListFlags.String("is-active", "", "")

		assignmentsGetFlags  = flag.NewFlagSet("get", flag.ExitOnError)
		assignmentsGetIDFlag = assignmentsGetFlags.String("id", "REQUIRED", "ID закрепления")
	)
	nomenclaturesFlags.Usage = nomenclaturesUsage
	nomenclaturesListFlags.Usage = nomenclaturesListUsage
	nomenclaturesGetFlags.Usage = nomenclaturesGetUsage

	departmentsFlags.Usage = departmentsUsage
	departmentsListFlags.Usage = departmentsListUsage
	departmentsGetFlags.Usage = departmentsGetUsage

	cardsFlags.Usage = cardsUsage
	cardsListFlags.Usage = cardsListUsage
	cardsGetFlags.Usage = cardsGetUsage
	cardsCreateFlags.Usage = cardsCreateUsage
	cardsUpdateFlags.Usage = cardsUpdateUsage
	cardsDeleteFlags.Usage = cardsDeleteUsage

	equipmentsFlags.Usage = equipmentsUsage
	equipmentsListFlags.Usage = equipmentsListUsage
	equipmentsGetFlags.Usage = equipmentsGetUsage
	equipmentsCreateFlags.Usage = equipmentsCreateUsage
	equipmentsUpdateFlags.Usage = equipmentsUpdateUsage
	equipmentsDeleteFlags.Usage = equipmentsDeleteUsage

	waybillsFlags.Usage = waybillsUsage
	waybillsListFlags.Usage = waybillsListUsage
	waybillsGetFlags.Usage = waybillsGetUsage
	waybillsCreateFlags.Usage = waybillsCreateUsage
	waybillsSignFlags.Usage = waybillsSignUsage
	waybillsArchiveFlags.Usage = waybillsArchiveUsage
	waybillsDeleteFlags.Usage = waybillsDeleteUsage

	assignmentsFlags.Usage = assignmentsUsage
	assignmentsListFlags.Usage = assignmentsListUsage
	assignmentsGetFlags.Usage = assignmentsGetUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "nomenclatures":
			svcf = nomenclaturesFlags
		case "departments":
			svcf = departmentsFlags
		case "cards":
			svcf = cardsFlags
		case "equipments":
			svcf = equipmentsFlags
		case "waybills":
			svcf = waybillsFlags
		case "assignments":
			svcf = assignmentsFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "nomenclatures":
			switch epn {
			case "list":
				epf = nomenclaturesListFlags

			case "get":
				epf = nomenclaturesGetFlags

			}

		case "departments":
			switch epn {
			case "list":
				epf = departmentsListFlags

			case "get":
				epf = departmentsGetFlags

			}

		case "cards":
			switch epn {
			case "list":
				epf = cardsListFlags

			case "get":
				epf = cardsGetFlags

			case "create":
				epf = cardsCreateFlags

			case "update":
				epf = cardsUpdateFlags

			case "delete":
				epf = cardsDeleteFlags

			}

		case "equipments":
			switch epn {
			case "list":
				epf = equipmentsListFlags

			case "get":
				epf = equipmentsGetFlags

			case "create":
				epf = equipmentsCreateFlags

			case "update":
				epf = equipmentsUpdateFlags

			case "delete":
				epf = equipmentsDeleteFlags

			}

		case "waybills":
			switch epn {
			case "list":
				epf = waybillsListFlags

			case "get":
				epf = waybillsGetFlags

			case "create":
				epf = waybillsCreateFlags

			case "sign":
				epf = waybillsSignFlags

			case "archive":
				epf = waybillsArchiveFlags

			case "delete":
				epf = waybillsDeleteFlags

			}

		case "assignments":
			switch epn {
			case "list":
				epf = assignmentsListFlags

			case "get":
				epf = assignmentsGetFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "nomenclatures":
			c := nomenclaturesc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "get":
				endpoint = c.Get()
				data, err = nomenclaturesc.BuildGetPayload(*nomenclaturesGetIDFlag)
			}
		case "departments":
			c := departmentsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "get":
				endpoint = c.Get()
				data, err = departmentsc.BuildGetPayload(*departmentsGetCodeFlag)
			}
		case "cards":
			c := cardsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "get":
				endpoint = c.Get()
				data, err = cardsc.BuildGetPayload(*cardsGetNumberFlag)
			case "create":
				endpoint = c.Create()
				data, err = cardsc.BuildCreatePayload(*cardsCreateBodyFlag)
			case "update":
				endpoint = c.Update()
				data, err = cardsc.BuildUpdatePayload(*cardsUpdateBodyFlag, *cardsUpdateNumberFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = cardsc.BuildDeletePayload(*cardsDeleteNumberFlag)
			}
		case "equipments":
			c := equipmentsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = equipmentsc.BuildListPayload(*equipmentsListStatusFlag, *equipmentsListNomenclatureIDFlag, *equipmentsListLocationFlag, *equipmentsListSearchFlag)
			case "get":
				endpoint = c.Get()
				data, err = equipmentsc.BuildGetPayload(*equipmentsGetInventoryNumberFlag)
			case "create":
				endpoint = c.Create()
				data, err = equipmentsc.BuildCreatePayload(*equipmentsCreateBodyFlag)
			case "update":
				endpoint = c.Update()
				data, err = equipmentsc.BuildUpdatePayload(*equipmentsUpdateBodyFlag, *equipmentsUpdateInventoryNumberFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = equipmentsc.BuildDeletePayload(*equipmentsDeleteInventoryNumberFlag)
			}
		case "waybills":
			c := waybillsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
			case "get":
				endpoint = c.Get()
				data, err = waybillsc.BuildGetPayload(*waybillsGetIDFlag)
			case "create":
				endpoint = c.Create()
				data, err = waybillsc.BuildCreatePayload(*waybillsCreateBodyFlag)
			case "sign":
				endpoint = c.Sign()
				data, err = waybillsc.BuildSignPayload(*waybillsSignIDFlag)
			case "archive":
				endpoint = c.Archive()
				data, err = waybillsc.BuildArchivePayload(*waybillsArchiveIDFlag)
			case "delete":
				endpoint = c.Delete()
				data, err = waybillsc.BuildDeletePayload(*waybillsDeleteIDFlag)
			}
		case "assignments":
			c := assignmentsc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "list":
				endpoint = c.List()
				data, err = assignmentsc.BuildListPayload(*assignmentsListEquipmentIDFlag, *assignmentsListIsActiveFlag)
			case "get":
				endpoint = c.Get()
				data, err = assignmentsc.BuildGetPayload(*assignmentsGetIDFlag)
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// nomenclaturesUsage displays the usage of the nomenclatures command and its
// subcommands.
func nomenclaturesUsage() {
	fmt.Fprintln(os.Stderr, `Справочник номенклатур (только чтение)`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] nomenclatures COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: Список всех номенклатур`)
	fmt.Fprintln(os.Stderr, `    get: Получить номенклатуру по ID`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s nomenclatures COMMAND --help\n", os.Args[0])
}
func nomenclaturesListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] nomenclatures list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Список всех номенклатур`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "nomenclatures list")
}

func nomenclaturesGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] nomenclatures get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Получить номенклатуру по ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT32: ID номенклатуры`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "nomenclatures get --id 968267662")
}

// departmentsUsage displays the usage of the departments command and its
// subcommands.
func departmentsUsage() {
	fmt.Fprintln(os.Stderr, `Справочник подразделений (только чтение)`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] departments COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: Список всех подразделений`)
	fmt.Fprintln(os.Stderr, `    get: Получить подразделение по коду`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s departments COMMAND --help\n", os.Args[0])
}
func departmentsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] departments list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Список всех подразделений`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "departments list")
}

func departmentsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] departments get", os.Args[0])
	fmt.Fprint(os.Stderr, " -code INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Получить подразделение по коду`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -code INT32: Код подразделения`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "departments get --code 1383710325")
}

// cardsUsage displays the usage of the cards command and its subcommands.
func cardsUsage() {
	fmt.Fprintln(os.Stderr, `Управление карточками сотрудников`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] cards COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: Список всех карточек`)
	fmt.Fprintln(os.Stderr, `    get: Получить карточку по номеру`)
	fmt.Fprintln(os.Stderr, `    create: Создать карточку`)
	fmt.Fprintln(os.Stderr, `    update: Обновить карточку`)
	fmt.Fprintln(os.Stderr, `    delete: Удалить карточку`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s cards COMMAND --help\n", os.Args[0])
}
func cardsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] cards list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Список всех карточек`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "cards list")
}

func cardsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] cards get", os.Args[0])
	fmt.Fprint(os.Stderr, " -number INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Получить карточку по номеру`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -number INT32: Номер карточки`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "cards get --number 251941874")
}

func cardsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] cards create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Создать карточку`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "cards create --body '{\n      \"full_name\": \"Repudiandae nihil repellendus in est.\",\n      \"number\": 48089892\n   }'")
}

func cardsUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] cards update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -number INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Обновить карточку`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -number INT32: Номер карточки`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "cards update --body '{\n      \"full_name\": \"Nam eaque similique suscipit suscipit.\"\n   }' --number 1758824794")
}

func cardsDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] cards delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -number INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Удалить карточку`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -number INT32: Номер карточки`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "cards delete --number 1524586912")
}

// equipmentsUsage displays the usage of the equipments command and its
// subcommands.
func equipmentsUsage() {
	fmt.Fprintln(os.Stderr, `Управление оборудованием`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] equipments COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: Список оборудования с фильтрацией`)
	fmt.Fprintln(os.Stderr, `    get: Получить оборудование по инвентарному номеру (с закреплением)`)
	fmt.Fprintln(os.Stderr, `    create: Создать оборудование`)
	fmt.Fprintln(os.Stderr, `    update: Обновить оборудование по инвентарному номеру`)
	fmt.Fprintln(os.Stderr, `    delete: Удалить оборудование (мягкое удаление)`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s equipments COMMAND --help\n", os.Args[0])
}
func equipmentsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] equipments list", os.Args[0])
	fmt.Fprint(os.Stderr, " -status STRING")
	fmt.Fprint(os.Stderr, " -nomenclature-id INT32")
	fmt.Fprint(os.Stderr, " -location STRING")
	fmt.Fprint(os.Stderr, " -search STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Список оборудования с фильтрацией`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -status STRING: `)
	fmt.Fprintln(os.Stderr, `    -nomenclature-id INT32: `)
	fmt.Fprintln(os.Stderr, `    -location STRING: `)
	fmt.Fprintln(os.Stderr, `    -search STRING: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "equipments list --status \"broken\" --nomenclature-id 1958819844 --location \"Laboriosam nemo rerum et aspernatur quia.\" --search \"Voluptas quia minus soluta.\"")
}

func equipmentsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] equipments get", os.Args[0])
	fmt.Fprint(os.Stderr, " -inventory-number STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Получить оборудование по инвентарному номеру (с закреплением)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -inventory-number STRING: Инвентарный номер`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "equipments get --inventory-number \"Ipsa et enim non numquam eum sint.\"")
}

func equipmentsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] equipments create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Создать оборудование`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "equipments create --body '{\n      \"arrival_date\": \"1973-06-12\",\n      \"form_number\": \"Labore ullam voluptas modi laboriosam.\",\n      \"inventory_number\": \"Recusandae ullam vitae aperiam dicta.\",\n      \"location\": \"Consequatur quisquam aut temporibus.\",\n      \"manufacture_date\": \"2012-11-12\",\n      \"model_name\": \"Ratione aut aspernatur qui quis et quo.\",\n      \"nomenclature_id\": 1292076222,\n      \"notes\": \"Nihil nemo.\",\n      \"serial_number\": \"Molestiae esse sit.\",\n      \"status\": \"broken\"\n   }'")
}

func equipmentsUpdateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] equipments update", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprint(os.Stderr, " -inventory-number STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Обновить оборудование по инвентарному номеру`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)
	fmt.Fprintln(os.Stderr, `    -inventory-number STRING: Инвентарный номер`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "equipments update --body '{\n      \"arrival_date\": \"1991-12-29\",\n      \"form_number\": \"Qui deserunt sed.\",\n      \"location\": \"Sequi officia.\",\n      \"manufacture_date\": \"1970-04-14\",\n      \"model_name\": \"Assumenda nam et molestiae nostrum temporibus qui.\",\n      \"nomenclature_id\": 925210294,\n      \"notes\": \"Dignissimos nisi non excepturi adipisci.\",\n      \"serial_number\": \"Saepe facere ex explicabo.\",\n      \"status\": \"written_off\"\n   }' --inventory-number \"Quo quo.\"")
}

func equipmentsDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] equipments delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -inventory-number STRING")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Удалить оборудование (мягкое удаление)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -inventory-number STRING: Инвентарный номер`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "equipments delete --inventory-number \"In architecto quia.\"")
}

// waybillsUsage displays the usage of the waybills command and its subcommands.
func waybillsUsage() {
	fmt.Fprintln(os.Stderr, `Управление накладными`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] waybills COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: Список накладных`)
	fmt.Fprintln(os.Stderr, `    get: Получить накладную по ID (с позициями)`)
	fmt.Fprintln(os.Stderr, `    create: Создать накладную (статус draft)`)
	fmt.Fprintln(os.Stderr, `    sign: Подписать накладную (draft → signed). Создаёт записи закреплений`)
	fmt.Fprintln(os.Stderr, `    archive: Архивировать накладную (signed → archived)`)
	fmt.Fprintln(os.Stderr, `    delete: Удалить накладную (только draft)`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s waybills COMMAND --help\n", os.Args[0])
}
func waybillsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] waybills list", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Список накладных`)

	// Flags list

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "waybills list")
}

func waybillsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] waybills get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Получить накладную по ID (с позициями)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT32: ID накладной`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "waybills get --id 1247690378")
}

func waybillsCreateUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] waybills create", os.Args[0])
	fmt.Fprint(os.Stderr, " -body JSON")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Создать накладную (статус draft)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -body JSON: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "waybills create --body '{\n      \"from_dept\": 1103443439,\n      \"issue_date\": \"2015-08-27\",\n      \"items\": [\n         {\n            \"equipment_id\": 2016065243,\n            \"waybill_id\": 664585174\n         },\n         {\n            \"equipment_id\": 2016065243,\n            \"waybill_id\": 664585174\n         },\n         {\n            \"equipment_id\": 2016065243,\n            \"waybill_id\": 664585174\n         },\n         {\n            \"equipment_id\": 2016065243,\n            \"waybill_id\": 664585174\n         }\n      ],\n      \"number\": \"Non quos maiores dolor ipsa.\",\n      \"to_dept\": 56768443\n   }'")
}

func waybillsSignUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] waybills sign", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Подписать накладную (draft → signed). Создаёт записи закреплений`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT32: ID накладной`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "waybills sign --id 1905838473")
}

func waybillsArchiveUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] waybills archive", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Архивировать накладную (signed → archived)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT32: ID накладной`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "waybills archive --id 772482203")
}

func waybillsDeleteUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] waybills delete", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Удалить накладную (только draft)`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT32: ID накладной`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "waybills delete --id 785219184")
}

// assignmentsUsage displays the usage of the assignments command and its
// subcommands.
func assignmentsUsage() {
	fmt.Fprintln(os.Stderr, `История закреплений оборудования`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] assignments COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    list: Список закреплений с фильтрацией`)
	fmt.Fprintln(os.Stderr, `    get: Получить закрепление по ID`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s assignments COMMAND --help\n", os.Args[0])
}
func assignmentsListUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] assignments list", os.Args[0])
	fmt.Fprint(os.Stderr, " -equipment-id INT32")
	fmt.Fprint(os.Stderr, " -is-active BOOL")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Список закреплений с фильтрацией`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -equipment-id INT32: `)
	fmt.Fprintln(os.Stderr, `    -is-active BOOL: `)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "assignments list --equipment-id 1097903018 --is-active false")
}

func assignmentsGetUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] assignments get", os.Args[0])
	fmt.Fprint(os.Stderr, " -id INT32")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Получить закрепление по ID`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -id INT32: ID закрепления`)

	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], "assignments get --id 683885988")
}
